# Copy this file to ".env" (same folder) and fill in your real key.
# .env is gitignored — it will NOT be committed. Never share or commit the real key.

GEMINI_API_KEY=paste_your_gemini_api_key_here
